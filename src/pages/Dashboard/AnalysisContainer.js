import React, { Fragment } from 'react';

function AnalysisContainer(props){
    return (
        <Fragment>
            {props.children}
        </Fragment>
    );
}

export default AnalysisContainer;